# Exercise 1: Setting Up JUnit

A basic test module to make sure the testing environment works properly in the project.

## Key Steps:

- Setup
  Checked that all the required testing files (jar archives) loaded correctly.

- Script
  Created SetupTest.java to run a simple test checking if two values match.